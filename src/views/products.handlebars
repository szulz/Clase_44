<button onclick="logOut()">Log Out</button> <br> <br>
<a href="/profile">👥PROFILE</a>||<a href="/carts/{{cartId}}">🛒 GO TO CART</a> ||<a href="/mail">📧 CONTACT US</a> ||
<a href="/sms">💬 SMS</a> || <a href="/chat">🗣️ CHAT</a><br>

<br>
{{#each products}}
<div>
    {{#if this.picture_filename}}
    <img src="/store/{{this.picture_filename}}" style="width: 100px; height: 100px;"><br>
    {{/if}}
    {{this.title}} <br>
    {{this.description}} <br>
    {{this.price}}$ <br>
    Stock: {{this.stock}} <br>
    <button onclick="addProduct({{this._id}})">ADD TO CART</button>
    <br>

</div>
{{/each}}


Page: {{#if getAll.prevLink}} <a href="{{getAll.prevLink}}">prev</a> {{/if}} {{getAll.page}} {{#if
getAll.nextLink}}<a href="{{getAll.nextLink}}">next</a>{{/if}}

<br>

<script src="/js/views.js"></script>